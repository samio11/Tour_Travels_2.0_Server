<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Booking Confirmation</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #f7f7f7;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 600px;
        margin: auto;
        background: #fff;
        padding: 20px;
        border-radius: 6px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
      }
      h1 {
        color: #2b6cb0;
      }
      img {
        max-width: 100%;
        border-radius: 6px;
        margin-bottom: 15px;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 15px;
      }
      table td {
        padding: 8px 5px;
        border-bottom: 1px solid #eee;
      }
      table td:first-child {
        font-weight: bold;
        width: 40%;
      }
      .btn {
        display: inline-block;
        margin-top: 15px;
        padding: 10px 16px;
        background: #38a169;
        color: white;
        text-decoration: none;
        border-radius: 4px;
      }
      .footer {
        font-size: 12px;
        text-align: center;
        color: #888;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Booking Created Successfully!</h1>
      <p>
        Hi <%= booking.user.name %>, your booking for
        <strong><%= booking.tour.title %></strong> has been created.
      </p>

      <img src="<%= booking.tour.images[0] %>" alt="Tour Image" />

      <table>
        <tr>
          <td>Booking ID</td>
          <td><%= booking._id %></td>
        </tr>
        <tr>
          <td>Tour Location</td>
          <td><%= booking.tour.location %></td>
        </tr>
        <tr>
          <td>Start Date</td>
          <td><%= new Date(booking.tour.startDate).toDateString() %></td>
        </tr>
        <tr>
          <td>End Date</td>
          <td><%= new Date(booking.tour.endDate).toDateString() %></td>
        </tr>
        <tr>
          <td>Guest Count</td>
          <td><%= booking.guestCount %></td>
        </tr>
        <tr>
          <td>Total Price</td>
          <td>৳<%= booking.payment.amount.toLocaleString() %> BDT</td>
        </tr>
        <tr>
          <td>Payment Status</td>
          <td><%= booking.payment.status %></td>
        </tr>
      </table>

      <% if (booking.payment.status === "UNPAID") { %>
      <a href="<%= paymentUrl %>" class="btn">Complete Payment</a>
      <% } %>

      <p style="margin-top: 20px">
        We look forward to having you on this amazing trip! 🎉
      </p>

      <div class="footer">
        &copy; <%= new Date().getFullYear() %> Tour & Travels. All rights
        reserved.
      </div>
    </div>
  </body>
</html>
